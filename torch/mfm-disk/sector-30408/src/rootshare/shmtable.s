 # Jump table for ROOTshare library routines. This must be loaded at
 # the beginning of the shared memory segment. The branches must
 # all have long displacements to ensure that the offsets within the
 # table remain constant. The lines are *not* free format as this
 # file is also used as the input to a script to calculate the entry
 # point for each routine. Unused slots should jump to "nofunc" which
 # will do its best to tell the user (s)he has screwed up.

	jmp	_bufsync	# 0
	jmp	_cleanup	# 1
	jmp	_doprnt		# 2
	jmp	_doscan		# 3
	jmp	_exit		# 4
	jmp	_filbuf		# 5
	jmp	_findbuf	# 6
	jmp	_findiop	# 7
	jmp	_flsbuf		# 8
	jmp	_sigcall	# 9
	jmp	_sigcode	# 10
	jmp	_signal		# 11
	jmp	_sysm68k	# 12
	jmp	_wrtchk		# 13
	jmp	_xflsbuf	# 14
	jmp	abs		# 15
	jmp	access		# 16
	jmp	acct		# 17
	jmp	alarm		# 18
	jmp	atof		# 19
	jmp	atoi		# 20
	jmp	atol		# 21
	jmp	blt		# 22
	jmp	blt512		# 23
	jmp	brk		# 24
	jmp	bsearch		# 25
	jmp	calloc		# 26
	jmp	cerror%		# 27
	jmp	cfree		# 28
	jmp	chdir		# 29
	jmp	chmod		# 30
	jmp	chown		# 31
	jmp	chroot		# 32
	jmp	clear		# 33
	jmp	clearerr	# 34
	jmp	close		# 35
	jmp	creat		# 36
	jmp	dbadd%%		# 37
	jmp	dbcmp%%		# 38
	jmp	dbdiv%%		# 39
	jmp	dbmul%%		# 40
	jmp	dbneg%%		# 41
	jmp	dbsub%%		# 42
	jmp	dbtofl%%	# 43
	jmp	dbtol%%		# 44
	jmp	dbtoul%%	# 45
	jmp	dbtst%%		# 46
	jmp	dup		# 47
	jmp	ecvt		# 48
	jmp	execl		# 49
	jmp	execle		# 50
	jmp	execv		# 51
	jmp	execve		# 52
	jmp	exit		# 53
	jmp	fclose		# 54
	jmp	fcntl		# 55
	jmp	fcvt		# 56
	jmp	fdopen		# 57
	jmp	fflush		# 58
	jmp	fgetc		# 59
	jmp	fgets		# 60
	jmp	fltodb%%	# 61
	jmp	fopen		# 62
	jmp	fork		# 63
	jmp	fprintf		# 64
	jmp	fputc		# 65
	jmp	fputs		# 66
	jmp	fread		# 67
	jmp	free		# 68
	jmp	freopen		# 69
	jmp	frexp		# 70
	jmp	fscanf		# 71
	jmp	fseek		# 72
	jmp	ofstat		# 73 old fstat (syscall 28)
	jmp	ftell		# 74
	jmp	ftok		# 75
	jmp	fwrite		# 76
	jmp	gcvt		# 77
	jmp	getchar		# 78
	jmp	getegid		# 79
	jmp	getenv		# 80
	jmp	geteuid		# 81
	jmp	getgid		# 82
	jmp	getpgrp		# 83
	jmp	getpid		# 84
	jmp	getppid		# 85
	jmp	gets		# 86
	jmp	getuid		# 87
	jmp	getw		# 88
	jmp	nofunc		# 89	was gtty
	jmp	ioctl		# 90
	jmp	isatty		# 91
	jmp	kill		# 92
	jmp	l3tol		# 93
	jmp	ldexp		# 94
	jmp	ldiv%%		# 95
	jmp	lfind		# 96
	jmp	link		# 97
	jmp	lmul%%		# 98
	jmp	lockf		# 99
	jmp	locking		# 100
	jmp	longjmp		# 101
	jmp	lrem%%		# 102
	jmp	lsearch		# 103
	jmp	lseek		# 104
	jmp	ltodb%%		# 105
	jmp	ltol3		# 106
	jmp	malloc		# 107
	jmp	memccpy		# 108
	jmp	memchr		# 109
	jmp	memcmp		# 110
	jmp	memcpy		# 111
	jmp	memset		# 112
	jmp	mknod		# 113
	jmp	mktemp		# 114
	jmp	modf		# 115
	jmp	mount		# 116 old mount (syscall 21) gets emulation
	jmp	msgctl		# 117
	jmp	msgget		# 118
	jmp	msgrcv		# 119
	jmp	msgsnd		# 120
	jmp	nice		# 121
	jmp	open		# 122
	jmp	pause		# 123
	jmp	perror		# 124
	jmp	phys		# 125
	jmp	pipe		# 126
	jmp	plock		# 127
	jmp	printf		# 128
	jmp	profil		# 129
	jmp	ptrace		# 130
	jmp	putchar		# 131
	jmp	puts		# 132
	jmp	putw		# 133
	jmp	read		# 134
	jmp	realloc		# 135
	jmp	reboot		# 136
	jmp	rewind		# 137
	jmp	sbrk		# 138
	jmp	scanf		# 139
	jmp	semctl		# 140
	jmp	semget		# 141
	jmp	semop		# 142
	jmp	setbuf		# 143
	jmp	setgid		# 144
	jmp	setjmp		# 145
	jmp	setpgrp		# 146
	jmp	setuid		# 147
	jmp	setvbuf		# 148
	jmp	shmat		# 149
	jmp	shmctl		# 150
	jmp	shmdt		# 151
	jmp	shmget		# 152
	jmp	signal		# 153
	jmp	sprintf		# 154
	jmp	sscanf		# 155
	jmp	ostat		# 156 old stat (syscall 18)
	jmp	stime		# 157
	jmp	strcat		# 158
	jmp	strchr		# 159
	jmp	strcmp		# 160
	jmp	strcpy		# 161
	jmp	strcspn		# 162
	jmp	strlen		# 163
	jmp	strncat		# 164
	jmp	strncmp		# 165
	jmp	strncpy		# 166
	jmp	strpbrk		# 167
	jmp	strrchr		# 168
	jmp	strspn		# 169
	jmp	strtod		# 170
	jmp	nofunc		# 171	was stty
	jmp	swab		# 172
	jmp	sync		# 173
	jmp	syscall		# 174
	jmp	sysm68k		# 175
	jmp	system		# 176
	jmp	tell		# 177
	jmp	tfind		# 178
	jmp	time		# 179
	jmp	times		# 180
	jmp	tolower		# 181
	jmp	toupper		# 182
	jmp	uldiv%%		# 183
	jmp	ulimit		# 184
	jmp	ulrem%%		# 185
	jmp	ultodb%%	# 186
	jmp	umask		# 187
	jmp	unmount		# 188 syscall 22 (was umount)
	jmp	uname		# 189
	jmp	ungetc		# 190
	jmp	unlink		# 191
	jmp	ustat		# 192
	jmp	utime		# 193
	jmp	uvar		# 194
	jmp	vfprintf	# 195
	jmp	vprintf		# 196
	jmp	vsprintf	# 197
	jmp	wait		# 198
	jmp	write		# 199
# BNET sys calls
	jmp	accept		# 200
	jmp	bind		# 201
	jmp	connect		# 202
	jmp	gethostid	# 203
	jmp	gethostname	# 204
	jmp	getpeername	# 205
	jmp	getsockname	# 206
	jmp	getsockopt	# 207
	jmp	listen		# 208
	jmp	recv		# 209
	jmp	recvfrom	# 210
	jmp	recvmsg		# 211
	jmp	select		# 212
	jmp	send		# 213
	jmp	sendmsg		# 214
	jmp	sendto		# 215
	jmp	sethostid	# 216
	jmp	sethostname	# 217
	jmp	setregid	# 218
	jmp	setreuid	# 219
	jmp	setsockopt	# 220
	jmp	shutdown	# 221
	jmp	socket		# 222
	jmp	socketpair	# 223
# NFS system calls
	jmp	getdomainname	# 224
	jmp	setdomainname	# 225
	jmp	getgroups	# 226
	jmp	setgroups	# 227
	jmp	getdtablesize	# 228
	jmp	flock		# 229
	jmp	readv		# 230
	jmp	writev		# 231
	jmp	mkdir		# 232
	jmp	rmdir		# 233
	jmp	getdirentries	# 234
	jmp	lstat		# 235
	jmp	symlink		# 236
	jmp	readlink	# 237
	jmp	truncate	# 238
	jmp	ftruncate	# 239
	jmp	fsync		# 240
	jmp	statfs		# 241
	jmp	fstatfs		# 242
	jmp	async_daemon	# 243
	jmp	nfssvc		# 244
	jmp	nfs_getfh	# 245
	jmp	rename		# 246
	jmp	fstat		# 247 new fstat (syscall 124)
	jmp	stat		# 248 new stat (syscall 125)
	jmp	vtrace		# 249
	jmp	getcompat	# 250
	jmp	setcompat	# 251
	jmp	sigvec		# 252
	jmp	sigblock	# 253
	jmp	sigsetmask	# 254
	jmp	sigpause	# 255
	jmp	sigstack	# 256
	jmp	getitmer	# 257
	jmp	setitmer	# 258
	jmp	_gettimeofday	# 259
	jmp	_settimeofday	# 260
	jmp	nofunc		# 261 should be adjtime (syscall 138)
	jmp	nofunc		# 262 should be quotactl (syscall 139)
	jmp	nofunc		# 263 should be exportfs (syscall 140)
	jmp	fsmount		# 264
	jmp	bzero		# 265 now same code as clear
	jmp	set43sig	# 266 module contains setcompat
	jmp	nofunc		# 267
	jmp	nofunc		# 268
	jmp	nofunc		# 269
	jmp	nofunc		# 270
	jmp	nofunc		# 271
	jmp	nofunc		# 272
	jmp	nofunc		# 273
	jmp	nofunc		# 274
	jmp	nofunc		# 275
	jmp	nofunc		# 276
	jmp	nofunc		# 277
	jmp	nofunc		# 278
	jmp	nofunc		# 279
	jmp	nofunc		# 280
	jmp	nofunc		# 281
	jmp	nofunc		# 282
	jmp	nofunc		# 283
	jmp	nofunc		# 284
	jmp	nofunc		# 285
	jmp	nofunc		# 286
	jmp	nofunc		# 287
	jmp	nofunc		# 288
	jmp	nofunc		# 289
	jmp	nofunc		# 290
	jmp	nofunc		# 291
	jmp	nofunc		# 292
	jmp	nofunc		# 293
	jmp	nofunc		# 294
	jmp	nofunc		# 295
	jmp	nofunc		# 296
	jmp	nofunc		# 297
	jmp	nofunc		# 298
	jmp	nofunc		# 299
